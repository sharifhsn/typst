#set heading(numbering: "1.")

= DOCX validator heading <validator-heading>

This paragraph carries the DOCX validator semantic sentinel.

- First editable list item
- Second editable list item

#table(
  columns: 2,
  table.header[*Name*][*Value*],
  [Alpha], [One],
  [Beta], [Two],
)

The heading reference resolves here: @validator-heading.

$sum_(i=1)^n i$
